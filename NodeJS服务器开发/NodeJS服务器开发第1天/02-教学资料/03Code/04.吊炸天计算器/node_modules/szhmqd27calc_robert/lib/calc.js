const add = (x,y) => x +y;


const subtraction = (x,y) => x-y;


const multiply = (x,y) => x*y;


const division = (x,y) => x/y;




module.exports = {
    add,
    multiply,
    subtraction,
    division
}